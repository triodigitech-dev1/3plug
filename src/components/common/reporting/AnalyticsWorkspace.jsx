import React from "react";

export function AnalyticsWorkspace({ metricSet, timeRange, groupBy, onTimeRangeChange, onDrilldown }) {
  return (<section data-component="AnalyticsWorkspace" className="analytics-workspace" />);
}
