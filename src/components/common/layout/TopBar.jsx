import React from "react";

export function TopBar({ branch, user, onlineState, quickActions = [], onActionClick, onOpenCommandPalette }) {
  return (<header data-component="TopBar" className="top-bar" />);
}
