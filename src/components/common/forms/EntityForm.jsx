import React from "react";

export function EntityForm({ schema, initialValues, mode = "create", permissions, onSaveDraft, onSubmit, onCancel }) {
  return (<form data-component="EntityForm" className="entity-form" />);
}
