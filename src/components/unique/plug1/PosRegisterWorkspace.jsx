import React from "react";

export function PosRegisterWorkspace({ catalog = [], taxRules = [], activeSession, cartLines = [], paymentMethods = [], onAddLine, onRemoveLine, onSubmitPayment, onCloseShift }) {
  return (<section data-component="PosRegisterWorkspace" className="pos-register-workspace" />);
}
